repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --manifest-path=server/Cargo.toml --
        language: system
        types: [rust]
      - id: gofmt
        name: gofmt
        entry: gofmt -l -w
        language: system
        types: [go]
